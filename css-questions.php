CSS 3

border-radius 
border-image
box-shadow

nth-child(n) - возвращает элемент, который является n-ым по счету дочерным элементом исходного
nth-last-child(n) – выполняет то же, что и предыдущий селектор, но счет идет от последнего элемента
nth-of-type(n) - возвращает элемент, который является n-ым по счету sibling элементом того же типа